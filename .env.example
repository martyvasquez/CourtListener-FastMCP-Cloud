# CourtListener MCP Server - Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# REQUIRED - CourtListener API Configuration
# =============================================================================

# Your CourtListener API key (required for API access)
# Get your key from: https://www.courtlistener.com/api/rest-info/
COURT_LISTENER_API_KEY=your_api_key_here

# =============================================================================
# OPTIONAL - API Configuration
# =============================================================================

# CourtListener API base URL (default: https://www.courtlistener.com/api/rest/v4/)
COURTLISTENER_BASE_URL=https://www.courtlistener.com/api/rest/v4/

# API request timeout in seconds (default: 30)
COURTLISTENER_TIMEOUT=30

# =============================================================================
# OPTIONAL - Logging Configuration
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
COURTLISTENER_LOG_LEVEL=INFO

# Enable debug mode for detailed logging (default: false)
COURTLISTENER_DEBUG=false

# =============================================================================
# OPTIONAL - Server Configuration (Local Development Only)
# =============================================================================
# Note: These are NOT needed for FastMCP Cloud deployment
# FastMCP Cloud manages hosting automatically

# Server host (default: 0.0.0.0)
HOST=0.0.0.0

# Server port for MCP (default: 8785)
MCP_PORT=8785

# Environment mode: production, development (default: production)
ENVIRONMENT=production

# =============================================================================
# FastMCP Cloud Deployment Notes
# =============================================================================
# When deploying to FastMCP Cloud (https://fastmcp.cloud):
#
# 1. Add COURT_LISTENER_API_KEY in your project's environment variables
# 2. Optionally add any custom configuration variables from above
# 3. HOST, MCP_PORT, and ENVIRONMENT are handled automatically by FastMCP Cloud
#
# Your server will be available at:
#   https://your-project-name.fastmcp.app/mcp
